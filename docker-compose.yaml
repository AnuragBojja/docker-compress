networks:
  roboshop:
    driver: bridge

volumes:
  mysql_data:
  mongodb_data:
  redis_data:
  rabbitmq_data:

services:
  mongodb:
    build : 
      context : ./mongodb
      dockerfile: dockerfile
    image: anurag/mongodb:v1
    container_name: mongodb
    volumes:
      - mongodb_data:/data/db
    networks:
      - roboshop
  
  catalogue:
    build:
      context: ./catalogue
      dockerfile: dockerfile
    image: anurag/catalogue:v1
    container_name: catalogue
    networks:
      - roboshop
    depends_on:
      - mongodb
  
  redis:
    image: redis:7
    container_name: redis
    networks:
      - roboshop
    volumes:
      - redis_data:/data
  
  user:
    build:
      context: ./user
      dockerfile: dockerfile
    image: anurag/user:v1
    container_name: user
    networks:
      - roboshop
    depends_on:
      - mongodb
      - redis
    
  cart:
    build: 
      context: ./cart
      dockerfile: dockerfile
    image: anurag/cart:v1
    container_name: cart
    networks:
      - roboshop
    depends_on:
      - redis
      - catalogue
  
  mysql:
    build: 
      context: ./mysql
      dockerfile: dockerfile
    image: anurag/mysql:v1
    container_name: mysql
    networks:
      - roboshop
    volumes:
      - mysql_data:/var/lib/mysql
  
  shipping:
    build: 
      context: ./shipping
      dockerfile: dockerfile
    image: sanurag/hipping:v1
    container_name: shipping
    networks:
      - roboshop
    depends_on:
      - mysql
      - user
  
  rabbitmq:
    image: rabbitmq:3
    container_name: rabbitmq
    networks:
      - roboshop
    environment:
      - RABBITMQ_DEFAULT_USER=roboshop
      - RABBITMQ_DEFAULT_PASS=roboshop123
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
  
  payment:
    build:
      context: ./payment
      dockerfile: dockerfile
    image: anurag/payment:v1
    container_name: payment
    networks:
      - roboshop
    depends_on:
      - rabbitmq
      - user
      - cart
  
  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    image: anurag/frontend:v1
    container_name: frontend
    ports:
      - "80:80"
    networks:
      - roboshop
    depends_on:
      - catalogue
      - cart
      - shipping
      - payment
      - user

    
